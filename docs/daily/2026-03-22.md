# Daily Log: 2026-03-22

## Summary（要約）
- GitHub Actions（Claude Code）の導入とトラブルシュート完了
- `gh-issue` / `gh-pr` skill を作成し、Issue 管理フローを整備
- 学習ロードマップを Issue #2〜#5 として起票
- ブランチ戦略を GitHub Flow に決定し、`develop` → `main` を統合（PR #6）
- `feature/issue-2-langgraphjs-basics` ブランチで Issue #2 の準備完了

---

## Discussion（議論ログ）

### GitHub App インストールエラーの原因調査
- user: `/install-github-app` で `gh: Not Found (HTTP 404)` エラーが出る
- assistant: `gh auth status` でトークンスコープを確認 → `workflow` スコープが欠如していた
- 結論: `gh auth refresh -h github.com -s repo,workflow` で解決

### gh-issue skill の設計
- user: `gh` コマンドで Issue を作成する skill を作ってほしい
- assistant: `~/.claude/skills/gh-issue/SKILL.md` を作成。手順は「リポジトリ確認 → ラベル確認 → 内容整理 → 作成 → URL報告」の5ステップ
- `settings.json` に `Bash(gh issue:*)` / `Bash(gh label:*)` 等を追加
- skill あり/なしの違い: ラベル事前確認・情報を1回でまとめて質問・毎回同じ品質の Issue 作成

### 学習ロードマップの Issue 化
- user: `docs/learning-roamap.md` の内容を複数 Issue に分けて起票してほしい
- assistant: 4 Issue に分割（Week1-Day1/Day2、Week2-Day1/Day2）
- `learning` ラベルを新規作成してアサイン
- Issue #2〜#5 として起票完了

### GitHub Issue の参照確認
- user: Claude Code から GitHub Issue を参照できるか？
- assistant: `gh issue view N` で参照可能。確認は `--json` オプションで Projects classic エラーを回避
- #1 は Issue ではなく PR（GitHub Actions 導入 PR）だった

### ブランチ戦略の決定
- user: ソロ・ポートフォリオ用途でのブランチ戦略は？
- assistant: **GitHub Flow** を推奨（`main` + `feature/issue-N-*`）
- Git Flow（`develop` ブランチ）はソロには過剰。採用担当が PR 履歴を見たとき分かりやすい形を優先
- Phase 完了時にタグ（`v0.1.0` など）を打つ

### develop → main の統合
- user: `develop` を `origin/main` にリベースして PR を作りたい
- 手順: `git fetch origin` → `git rebase origin/main` → `git push --force-with-lease` → `gh pr create`
- PR #6 を作成・マージ完了

### claude-code-review.yml の仕組み確認
- user: PR 作成時に自動レビューが始まったのはなぜか？
- assistant: `claude-code-review.yml` に `on: pull_request: types: [opened, ...]` が定義されているため
- レビュー観点: CLAUDE.md compliance（2エージェント）+ バグ検出 + セキュリティ・ロジック問題（各 Opus）
- **ハイシグナルのみ**フラグを立てる設計（コードスタイル・潜在的な問題はフラグなし）

### コードレビュー観点の定義
- user: レビュー観点を定義するなら CLAUDE.md に書くべき？
- assistant: そう。code-review プラグインが CLAUDE.md compliance agent を走らせるため
- 抽象的なルールより具体的な禁止事項の方が精度が上がる
- Issue #7 として起票し、後日議論して追記する方針に決定

### gh-pr skill の設計
- user: PR を作成する skill も作ってほしい
- assistant: `~/.claude/skills/gh-pr/SKILL.md` を作成
- gh-issue との差分: ブランチ状態確認・差分サマリー把握・`Closes #N` 自動付与の3点を追加
- `settings.json` に `Bash(gh pr:*)` を追加

---

## Done（やったこと）

- `/install-github-app` のトラブルシュート → `workflow` スコープ追加で解決
- `gh-issue` skill 作成（`~/.claude/skills/gh-issue/SKILL.md`）
- `gh-pr` skill 作成（`~/.claude/skills/gh-pr/SKILL.md`）
- `~/.claude/CLAUDE.md` に `gh-issue` / `gh-pr` を `available_skills` に登録
- `~/.claude/settings.json` に gh コマンド系パーミッションを追加
- `learning` ラベルを GitHub に作成
- Issue #2〜#5 起票（学習ロードマップ）
- Issue #7 起票（コードレビュー観点定義）
- `develop` → `origin/main` リベース → PR #6 作成・マージ
- `feature/issue-2-langgraphjs-basics` ブランチ作成・push
- `docs/learning-roamap.md` / `docs/design-code-review-skills.md` をコミット・push

---

## Decisions（決めたこと）

- ブランチ戦略: **GitHub Flow**（`main` + `feature/issue-N-*`）。`develop` ブランチは廃止
- コードレビュー観点の定義先: `CLAUDE.md`（Issue #7 で後日議論）
- PR 本文には `Closes #N` を必ず含める（Issue との紐付けをポートフォリオ観点で標準化）

---

## Notes（メモ / 学び）

- `gh issue view` は `--json` オプションを使うと Projects classic の deprecation エラーを回避できる
- `claude-code-review.yml` のレビューは「絶対に問題だと言えるものだけ」フラグを立てる設計。false positive を避けることを最優先にしている
- skill ファイルは `~/.claude/CLAUDE.md` の `available_skills` に登録しないと認識されない

---

## Blockers（詰まり / 課題）

- なし

---

## Next（明日やること）

- Issue #2 を開始: `feature/issue-2-langgraphjs-basics` で LangGraph.js を動かす
  - `git clone https://github.com/langchain-ai/langgraphjs.git`
  - `examples/intro/` を全部動かして StateGraph + node + edge を理解
  - `examples/multi_agent/` の State 設計をメモ
  - 内側エージェント処理フローを手書きで描く
- Issue #2 完了後 → `/gh-pr` で PR 作成（`Closes #2`）

---

## Links（関連リンク）

- PR: https://github.com/akihirookuda95/ai-news-agent/pull/6 （develop → main）
- Issues:
  - #2 https://github.com/akihirookuda95/ai-news-agent/issues/2 （Week1-Day1 LangGraph.js 基本）
  - #3 https://github.com/akihirookuda95/ai-news-agent/issues/3 （Week1-Day2 LangGraph.js 応用）
  - #4 https://github.com/akihirookuda95/ai-news-agent/issues/4 （Week2-Day1 MCP SDK 基本）
  - #5 https://github.com/akihirookuda95/ai-news-agent/issues/5 （Week2-Day2 MCP 実装）
  - #7 https://github.com/akihirookuda95/ai-news-agent/issues/7 （コードレビュー観点定義）
- Docs:
  - `docs/learning-roamap.md`
  - `docs/design-code-review-skills.md`
  - `Contexts/20260322_github-setup-and-skills.md`
- Files:
  - `~/.claude/skills/gh-issue/SKILL.md`
  - `~/.claude/skills/gh-pr/SKILL.md`
  - `~/.claude/CLAUDE.md`
  - `~/.claude/settings.json`
- Commits:
  - `d8cd857` — docs: プロジェクト開始前の学習ロードマップを追加
  - `3e20c64` — docs: 設計力・コードレビュー力の鍛え方メモを追加
  - `39db864` — Merge pull request #6
